[Unit]
Description=VNC server for the dedicated IB Gateway virtual display (:1)
After=ibgw-xvfb.service
Requires=ibgw-xvfb.service

[Service]
Type=simple
ExecStart=/usr/bin/x11vnc -nopw -localhost -display :1 -rfbport 5902 -forever
Restart=on-failure
RestartSec=5

[Install]
WantedBy=default.target
